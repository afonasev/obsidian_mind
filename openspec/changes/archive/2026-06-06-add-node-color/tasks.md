## 1. Модель и палитра

- [x] 1.1 Добавить `color?: string` в `NodeNameStyle` (`src/domain/types.ts`) с комментарием-WHY о двух видах значения (ключ пресета | hex) и «absent = дефолтная поверхность, без миграции»
- [x] 1.2 Завести реестр ключей пресетов и предикат `isPresetKey(value)` (единый источник имён для сетки и рендера); чистый TS, без React
- [x] 1.3 Добавить группу токенов `--node-fill-<name>` в `src/theme.css` в обе секции (`:root` и `[data-theme="dark"]`), имена ключей идентичны реестру из 1.2; оттенки читабельны под `--color-fg`

## 2. Домен: наследование и сброс

- [x] 2.1 В `graph.addChild` копировать снимок `parent.style?.color` в `style.color` нового узла (только цвет, не весь стиль)
- [x] 2.2 Поддержать в `updateNodeStyle` явную очистку `color` (сброс удаляет ключ, а не пишет `undefined`) с учётом `exactOptionalPropertyTypes`
- [x] 2.3 Юнит-тесты `graph.test.ts`: ребёнок наследует цвет родителя; родитель без цвета → ребёнок без цвета; перекраска родителя не трогает существующих детей; сброс убирает поле `color`

## 3. Последние использованные цвета (MRU)

- [x] 3.1 Утилита MRU над `localStorage` (отдельный ключ): чтение списка (≤5), `apply(color)` = unshift + dedup + срез до 5; значения — ключи и/или hex
- [x] 3.2 Хук `use…` через `useSyncExternalStore` (по образцу `useTheme`): текущий список + `apply`; стабильный тип
- [x] 3.3 Юнит-тесты MRU: применение добавляет в начало; повтор перемещает без дубля; длина ≤5; переживает «перезапуск» (повторное чтение)

## 4. UI: свотч и поповер

- [x] 4.1 Добавить кнопку-свотч в `FormatToolbar` (отражает текущий цвет узла); открытие/закрытие поповера; `onMouseDown + preventDefault + stopPropagation`, обёртка `nopan`
- [x] 4.2 Поповер: сетка пресетов, свотч сброса `⊘`, ряд недавних (из хука 3.2), кнопка «свой цвет…» с нативным `<input type="color">`
- [x] 4.3 Применение выбора → `setNodeStyle(id, { color })` (или сброс через 2.2) + `apply(color)` в MRU; закрытие поповера после выбора
- [x] 4.4 Рендер заливки облака в `CloudNode`/`CloudNode.module.css` из `color`: `isPresetKey ? var(--node-fill-…) : литерал`; применяется и в просмотре, и в инлайн-правке; панель-редактор и список корней — без пользовательского фона
- [x] 4.5 Селектор цвета узла в сторе/компоненте — примитивный, чтобы не ломать referential equality zustand (как у bold/italic/fontScale)

## 5. Тесты компонентов

- [x] 5.1 Свотч виден в тулбаре при правке и открывает поповер, не выходя из режима правки
- [x] 5.2 Выбор пресета красит заливку и попадает в недавние; выбор кастомного цвета через `<input type="color">` красит заливку
- [x] 5.3 Сброс `⊘` убирает цвет (узел возвращается к дефолтной поверхности)
- [x] 5.4 Пресет адаптируется при смене `data-theme`; кастомный hex одинаков в обеих темах
- [x] 5.5 Достичь 100% coverage по новому TS-коду (`bun run test`)

## 6. Документация

- [x] 6.1 `README.md` — цвет узлов в статусе MVP (новая возможность)
- [x] 6.2 `docs/frontend.md` — свотч/поповер в тулбаре, токены палитры `--node-fill-*`, MRU-хук
- [x] 6.3 `docs/storage.md` — поле `color` в `NodeNameStyle` (формат: ключ пресета | hex), absent = дефолт, без миграции
- [x] 6.4 `openspec validate add-node-color` и финальный `make check`
