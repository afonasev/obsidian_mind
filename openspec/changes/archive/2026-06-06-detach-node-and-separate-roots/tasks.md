## 1. Домен: открепление узла

- [x] 1.1 Добавить `detachAsRoot(graph, {nodeId, position})` в `src/domain/graph.ts`: валидация (узел существует, `parentId !== null`) с no-op-возвратом той же ссылки; при успехе `parentId → null`, новая `position`, удаление ребра с `target === nodeId`
- [x] 1.2 Тесты `graph.test.ts`: успешное открепление (узел стал корнем, ребро удалено, поддерево связно), no-op для корня и неизвестного узла (возврат той же ссылки)

## 2. Домен: порог «далеко» и разведение корней

- [x] 2.1 Добавить в `src/domain/layout.ts` хелпер-предикат «узел далеко от родителя»: `dist(node, parent) > estimateNodeWidth(parent) + LAYOUT_HGAP + K · LAYOUT_HGAP`, константа `K` (стартово 2) экспортируется
- [x] 2.2 Реализовать проход разрешения коллизий корней в `layout()`: bbox поддерева каждого корня, детерминированный порядок (по `y`, затем `id`), вертикальный сдвиг налезающего корня вместе с поддеревом до устранения пересечения, итерация до стабилизации
- [x] 2.3 Тесты `layout.test.ts`: пересекающиеся корни разводятся по вертикали; уже разнесённые корни при повторной раскладке не двигаются (идемпотентность); хелпер порога на граничных расстояниях (в покое не срабатывает, далеко срабатывает)

## 3. Стор: экшен detach и transient-флаг

- [x] 3.1 Добавить экшен `detach(nodeId, position)` в `src/store/mindmap-store.ts`: `graphOps.detachAsRoot` → при изменении `commit(relayout(...))` с коалесингом drag-а; no-op без истории при той же ссылке
- [x] 3.2 Добавить transient-поле кандидата на открепление (по аналогии с `dropTargetId`) + сеттер; сброс в `clearTransient`/конце drag
- [x] 3.3 Тесты `mindmap-store.test.tsx`: detach делает узел корнем за один шаг undo; флаг кандидата выставляется/сбрасывается

## 4. Канвас: развилка drop и пометка кандидата

- [x] 4.1 В `handleNodeDrag` (`Canvas.tsx`): при отсутствии цели и пройденном пороге выставлять флаг кандидата на открепление, иначе сбрасывать
- [x] 4.2 В `handleNodeDragStop`: третья ветка — нет цели + не корень + порог пройден → `detach(node.id, node.position)`, иначе прежнее поведение; сброс флага
- [x] 4.3 Тесты `Canvas.test` (если есть) / стора: drop далеко открепляет, drop близко притягивает обратно, drop над узлом по-прежнему перепривязывает

## 5. Визуал: рвущееся ребро

- [x] 5.1 Пометить ребро от родителя к перетаскиваемому узлу как «рвущееся» при активном флаге кандидата (в построении RF-рёбер) + стиль (пунктир/затухание) в CSS-модуле рёбер/`CloudNode`
- [x] 5.2 Тест: при флаге кандидата соответствующее ребро получает рвущийся класс/стиль, при сбросе — обычный

## 6. Документация

- [x] 6.1 Обновить `docs/frontend.md` (поведение drop: открепление, рвущееся ребро) и `docs/architecture.md`/`docs/storage.md` при необходимости (новое доменное поведение раскладки)
- [x] 6.2 Синхронизировать `openspec/specs/mindmap-editor/spec.md` с дельтой (через `/opsx:sync` или при архивации change)

## 7. Проверка

- [x] 7.1 `make check` зелёный (format + lint + type-check + test 100% coverage + e2e)
