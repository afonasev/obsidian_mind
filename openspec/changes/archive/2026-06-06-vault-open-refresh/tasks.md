## 1. Сессия vault в сторе

- [x] 1.1 Состояние сессии: `NoVault` / `Loaded` (флаг `hasVault`); поле активного vault
- [x] 1.2 Действие `openVault`: выбор папки (через `vault-access`) → чтение (через `vault-storage`) → `Loaded`; запоминание пути
- [x] 1.3 Авто-открытие последнего vault при старте; если папки нет — `NoVault`
- [x] 1.4 Действие `refreshFromDisk`: flush → re-read → reconcile по `id` (логика reconcile — из `vault-storage`)
- [x] 1.5 Per-vault: активное пространство в привязке к пути vault (состояние панели — глобальное, см. дизайн Решение 5)
- [x] 1.6 Тесты стора на in-memory `VaultStore`: открытие, авто-открытие, reconcile (тело/исчезновение пространства); ряды таблицы reconcile покрыты тестами `vault-storage`

## 2. Компоненты

- [x] 2.1 Пустое состояние «нет vault» с кнопкой «Открыть директорию-vault»
- [x] 2.2 Кнопка «Перечитать с диска» (видна в состоянии `Loaded`)
- [x] 2.3 Различить пустые состояния: «нет vault» vs «vault открыт, пространств нет»
- [x] 2.4 `WorkspacePanel` читает пространства активного vault
- [x] 2.5 Тесты компонентов: экран «нет vault», открытие, refresh-кнопка, два разных пустых состояния

## 3. E2E и документация

- [x] 3.1 Playwright e2e (на in-memory адаптере): старт без vault → открыть → создать пространство/узлы; внешняя правка содержимого → «Перечитать с диска» подхватывает изменения
- [x] 3.2 `README.md`: как открыть vault и перечитать с диска
- [x] 3.3 `docs/frontend.md` (состояние «нет vault», кнопка refresh), `docs/architecture.md` (жизненный цикл сессии vault)
- [x] 3.4 `make check` зелёный (format + lint + type-check + test 100% + e2e)
