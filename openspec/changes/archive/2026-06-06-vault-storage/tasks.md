## 1. Домен: маппинг граф↔файлы

- [x] 1.1 Модуль сериализации/парсинга вёрстки: `spaces.yaml`, `space.yaml`, `root.yaml` ↔ доменные типы (узлы, позиции, стиль, свёрнутость, дерево); чистые функции
- [x] 1.2 Разбор/сборка заметки: frontmatter `id` + markdown-тело ↔ `MindNode.body`
- [x] 1.3 `sanitize` имени файла из текста узла (запрещённые символы, пустое имя → дефолт, коллизия → суффикс по `id`)
- [x] 1.4 Мягкое чтение: по-элементные дефолты для битого/отсутствующего YAML (см. таблицу в design); восстановление дерева из `.md` при отсутствии `root.yaml`
- [x] 1.5 Тесты домена: round-trip сериализации, связка по `id`, каждый случай деградации из таблицы

## 2. Порт `VaultStore` и адаптеры

- [x] 2.1 Интерфейс `VaultStore` (загрузка пространств/корней/графа корня; сохранение узла/тела/вёрстки; CRUD пространства/корня/узла)
- [x] 2.2 FS-адаптер поверх команд `vault-access` (чтение дерева, read/write/rename/remove); запись текста через temp-file + rename
- [x] 2.3 In-memory адаптер (карта путь→содержимое) для тестов и web-сборки
- [x] 2.4 Тесты обоих адаптеров на общем наборе сценариев (контрактные тесты порта)

## 3. Persistence: IDB → app-prefs

- [x] 3.1 `db.ts`: убрать object stores контента (графы, пространства); оставить `meta` для app-prefs; активное пространство — ключом per-vault
- [x] 3.2 `repository.ts`: загрузка/сохранение графов и пространств — через `VaultStore`; app-prefs (last vault, ширины, тема, активное пространство) — в IDB
- [x] 3.3 Обновить тесты persistence под новый раздел ответственности

## 4. Store

- [x] 4.1 `mindmap-store.ts`: загрузка активного среза (граф корня) через `VaultStore`; сохранение мутаций — дебаунс-сейвер пишет затронутый `root.yaml` и файл заметки
- [x] 4.2 Жизненный цикл тела: появление тела → создать `.md` + `file`; удаление тела → удалить `.md` + убрать `file`; удаление узла → убрать запись + `.md`
- [x] 4.3 Синхронный `flush` перед сменой активного пространства/корня
- [x] 4.4 Тесты стора на in-memory `VaultStore`: создание/редактирование/удаление узла отражается в файлах; появление/исчезновение тела управляет файлом

## 5. Документация

- [x] 5.1 Переписать `docs/storage.md` под файловую модель: раскладка vault, формат `.mind/` (`spaces.yaml`/`space.yaml`/`root.yaml`), frontmatter заметки, sanitize, таблица мягкого чтения
- [x] 5.2 `docs/architecture.md`: порт `VaultStore` и адаптеры (FS / in-memory), разделение «контент в vault / app-prefs в IDB»
- [x] 5.3 `docs/decisions/2026-06-06_vault-as-source-of-truth.md`: что/зачем/цена (отказ от Canvas, `.mind/`, связка по `id`, отказ от миграции)
- [x] 5.4 `make check` зелёный (format + lint + type-check + test 100% + e2e)
